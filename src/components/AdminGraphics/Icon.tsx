import React from 'react'

/** Small CTG loop-mark shown in the admin nav / collapsed sidebar. */
export const Icon = () => (
  /* eslint-disable-next-line @next/next/no-img-element */
  <img src="/ctg-icon.png" alt="CTG" width={28} height={28} style={{ borderRadius: '50%', display: 'block' }} />
)

export default Icon
